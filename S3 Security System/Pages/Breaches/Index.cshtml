@page
@model S3_Security_System.Pages.Breaches.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Breach[0].S3_Security_SystemUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Breach[0].BreachType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Breach[0].BreachResp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Breach[0].DateAndTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Breach) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.S3_Security_SystemUser.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BreachType.BreachTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BreachResp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateAndTime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
