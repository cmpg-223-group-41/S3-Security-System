@page
@model S3_Security_System.Pages.BreachTypes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BreachType[0].BreachTypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BreachType[0].BreachDesc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.BreachType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BreachTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BreachDesc)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
